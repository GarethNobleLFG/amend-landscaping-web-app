services:

  api:
    image: node:20-alpine
    working_dir: /app
    ports:
      - "${PORT}:${PORT}"
    env_file:
      - .env 
    volumes:
      - ./api:/app 
      - /app/node_modules
    command: sh -c "npm install && npm run dev"